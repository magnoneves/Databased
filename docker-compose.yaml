version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DB_HOST=mysql.railway.internal
      - DB_USER=root
      - DB_PASSWORD=lpxhlmLQRETUaozUitqnOpqcQynTKZyN
      - DB_NAME=railway
      - DB_PORT=3306
      - PORT=3000
